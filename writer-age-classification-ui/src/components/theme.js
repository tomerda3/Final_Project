import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3f51b5', // Adjust the primary color as needed
    },
    secondary: {
      main: '#f50057', // Adjust the secondary color as needed
    },
  },
});

export default theme;
